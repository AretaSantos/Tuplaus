package com.tuplaus;

import org.springframework.stereotype.Controller;

/**
 * Luokka clientia varten
 * 
 * */
@Controller
public class TuplausController {

/*
	private static TuplausService tuplausService = new TuplausService();

	public TuplausController(TuplausService tuplausService) {

		TuplausController.tuplausService = tuplausService;
	}
	/*
	@PostMapping("/get") 
	public static Game get(Game game) {
		game = tuplausService.getPlayerInfo(game);
		return game;
	}

	//@PostMapping("/play") when http request used for frontend
	public static void play(Game game) {

		//tuplausService.playGame(game);

	}

	public static void enoughBalance(Game game) {
		tuplausService.enoughBalance(game);
		
	}*/

}
